<?php

namespace Tests\Unit\UseCase\Category;

use PHPUnit\Framework\TestCase;

class CreateCategoryUseCaseUnitTest extends TestCase
{
    public function testCreateNewCategory()
    {
/*        $categoryId = '';
        $categoryName = 'Name Cat';
        $categoryDescription =  'new description';


        $this->mockEntity = Mockery::mock(Category::class,[
            $categoryName,
            $categoryId,
            $categoryDescription
        ]);

        $this->mockRepo = Mockery::mock(sdtClass::class, CategoryRepositoryInterface::class );
      //  $this->mockRepo->shouldReceive('insert');//->andReturn($this->mockEntity);

        $useCase = new CreateCategoryUseCase($this->mockRepo);
        $useCase->execute();

        Mockery::close();*/
    }
}